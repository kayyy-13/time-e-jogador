<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Menu</title>
    <style>
        /* Estilos Gerais */
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            text-align: center;
        }

        /* Estilo do Cabeçalho */
        header {
            background-color: #28a745;
            color: white;
            width: 100%;
            padding: 30px 0;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            text-align: center;
            border-bottom: 5px solid #218838;
        }

        header h1 {
            font-size: 48px;
            margin: 0;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 2px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2); /* Sombra para o título */
        }

        /* Estilo dos Links */
        a {
            display: inline-block;
            margin: 15px;
            padding: 15px 25px;
            background-color: #28a745; 
            color: white;
            text-decoration: none;
            border-radius: 8px;
            font-size: 18px;
            transition: background-color 0.3s, transform 0.2s;
        }

        a:hover {
            background-color: #218838;
            transform: translateY(-5px);
        }

        /* Estilo adicional do Conteúdo */
        .content {
            margin-top: 20px;
        }

        /* Título */
        h1 {
            font-size: 32px;
            color: #333;
            margin-bottom: 30px;
        }
    </style>
</head>
<body>
    <!-- Cabeçalho -->
    <header>
        <h1>Bem-vindo!</h1>
    </header>

    <div class="content">
        <h1>Menu de navegação</h1>
        <a href="admin/time/lst">Times</a>
        <a href="admin/jogador/lst">Jogadores</a>
    </div>
</body>
</html>
